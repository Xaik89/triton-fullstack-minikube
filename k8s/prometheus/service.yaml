apiVersion: v1
kind: Service
metadata:
  name: prometheus
  namespace: triton-inference
  labels:
    app: prometheus
spec:
  type: NodePort
  ports:
  - port: 9090
    targetPort: 9090
    protocol: TCP
    name: http
    nodePort: 30090
  selector:
    app: prometheus

